# Changelog

## Unreleased

- 按新手安装顺序重写 README，补充隐藏密码输入、成功提示和任务验证步骤。
- 增加日常使用与排障指南，覆盖日志位置、升级、停用、卸载和下载校验；程序版本及行为不变。

## 1.2.0 — 2026-09-24

- 发布单文件一键部署版，并设为 GitHub Latest；普通安装只需 `AutoLogin_SIAS_Installer.exe`。
- 整合 rc.1 / rc.2 的任务安装、认证判断、凭据保真、文件替换和回滚修复；相较 rc.2 无新增运行逻辑。
- 首页仅展示当前版本流程，旧版双 EXE 步骤移入历史文档；发布源码、EXE 版本号和校验文件一致。
- 未完成的完整安装向导/UAC、全新账户、锁屏和重连验收仍在验证报告中明确列出。

## 1.2.0-rc.2

- 修复密码首尾空白被移除的问题；新配置使用带版本标记的 JSON 字符串编码，保留引号、反斜杠及空白。
- 继续兼容既有 `.env`；新格式配置需要 rc.2 或更高版本读取，回退旧程序时应同时恢复旧配置。
- 补充配置往返及实际 EXE 特殊字符加密回归测试。
- 安装时先写临时文件再替换，短暂文件占用时有限重试；相同程序无需重复覆盖，回滚只恢复实际替换过的文件。

## 1.2.0-rc.1

候选版本，尚待真实校园网、UAC 和计划任务端到端验收。

- 共用版本号；加入 Windows CI、固定构建依赖和发布包 SHA-256 清单。
- 拒绝将未知登录响应作为成功；正确处理布尔 `result` / `status` / `code`，新增退出码 8。
- 修复已有任务缺少工作目录时的更新，并在登录验证被取消时恢复旧安装文件。
- 精简安装器不使用的 `_hashlib` 依赖；后台程序保留 TLS 支持。

- 增加单文件部署安装包：内置后台 EXE 和任务脚本，输入账号密码后完成登录验证及计划任务安装。
- 安装到当前账户的固定本地目录；登录验证失败恢复旧文件，任务安装失败保留文件并明确提示。

- 增加 Windows 计划任务安装/更新脚本，支持 XML 预览和更新前备份。
- Wi-Fi 触发兼容 8001 / 11005，限定 SSID，延迟 30 秒并避免中断已有认证。
- 完善每日 04:10、旧定时触发保留、快速启动、睡眠补跑与排障说明。

## 1.1.0

- 增加 `AutoLogin_SIAS_Setup.exe` 首次配置向导。
- 支持安全输入密码并创建 `.env`。
- 增加 `--setup`、`--check`、`--version` 参数。
- 增加日志轮转和日志文件不可写时的兜底处理。
- 保留无参数静默模式，适合任务计划程序。
- 修正 EXE 与项目根目录 `.env` 的查找顺序。

## 1.0.0

- 首个无浏览器后台登录版本。
- 使用门户兼容的 RC4 密码转换和动态 `auth_tag`。
- 支持 `login.php` 与 `handler_checkjump` 请求流程。
